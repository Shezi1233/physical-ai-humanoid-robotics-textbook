# Frontend Environment Variables
# Note: Docusaurus does not read .env at runtime.
# The actual backend URL is set in static/config.js (loaded via <script>).
BACKEND_URL=https://shezi1344-physical-ai-chatbot.hf.space
